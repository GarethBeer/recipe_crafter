{"routes": [
    {
      "route": "/home",
      "allowedRoles": [
        "authenticated"
      ],
      "redirect": "/"
    }
  ],
  "navigationFallback": {
    "rewrite": "/index.html",
    "exclude": [
      "*.{css,scss,js,png,gif,ico,jpg,svg}"
    ]
  }
  }